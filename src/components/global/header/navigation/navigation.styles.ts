import { css } from '@/styled-system/css';

export const styles = {
  nav: css({
    display: 'flex',
    alignItems: 'center',
    blockSize: 'full'
  }),
  list: css({
    display: 'flex',
    alignItems: 'center',
    blockSize: 'full'
  }),
  item: css({
    display: 'flex',
    alignItems: 'center',
    blockSize: 'full'
  })
};
