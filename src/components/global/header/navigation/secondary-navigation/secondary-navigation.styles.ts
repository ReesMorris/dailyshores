import { css } from '@/styled-system/css';

export const styles = {
  list: css({
    display: 'flex',
    alignItems: 'center',
    blockSize: 'full'
  }),
  item: css({
    display: 'flex',
    alignItems: 'center',
    blockSize: 'full'
  })
};
