import { css } from '@/styled-system/css';

export const styles = {
  wrapper: css({
    inlineSize: 'full',
    maxInlineSize: 'breakpoint-lg',
    marginInline: 'auto',
    paddingInline: '4'
  })
};
